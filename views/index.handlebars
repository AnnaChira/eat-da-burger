<h1>Burgers</h1>

<h2>Burgers that have eaten</h2>

<ul>
  {{#each Burgers}}
    {{#unless devoured}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers have been eaten!</h2>

<ul>
  {{#each Burgers}}
    {{#if devoured}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>New Burgers</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burgers:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Devoured?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Devoured!<br>
    <input type="radio" name="sleepy" value="0"> Eat
  </div>

  <button type="submit">Add Burger</button>
</form>
